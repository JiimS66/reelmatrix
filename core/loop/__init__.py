"""Loop-engineering primitives — the engine behind the product's two loops (strategy
co-creation and marketing operations). A Loop is a recursive goal the agent iterates
toward, one VERIFIED step at a time, escalating to a human when human judgment is the
value. The guards (max_turns, explicit is_done) are the anti-'loopmaxxing' brakes."""
