"""Paid media (Phase 10): pre-spend creative scoring + a create→test→reallocate loop on
mock spend. Pluggable behind CreativeScoreProvider + BudgetAllocator; real ad APIs
(Meta Advantage+ / Google PMax) swap in later."""
