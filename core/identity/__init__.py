"""Phase 13 — the data foundation: identity resolution stitches fragmented records into
unified profiles (Segment Unify / RudderStack Profiles model). Deterministic union-find
now; probabilistic stitch + a warehouse-native CDP swap in later. The prerequisite for
real enterprise data — the system stops assuming one clean tenant table."""
